name: Legacy Frontend Check

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op legacy workflow
        run: echo "This workflow is kept only to satisfy stale editor diagnostics."
